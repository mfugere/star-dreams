function Attack() {
    /** TODO: Create this as a singleton with functions available for different attacks.
     * then add the attack type as a constructor arg to an Enemy instance,
     * along with an attack function to call their respective attack.
     **/
}